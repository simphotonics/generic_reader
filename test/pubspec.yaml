name: generic_reader_test
description:
  Test suite of library generic_reader.

environment:
  sdk: '>=2.8.2 <3.0.0'

dependencies:
  example:
    path: ../example
  generic_reader:
    path: ../
  source_gen: ^0.9.5
  source_gen_test: ^0.1.1
  test: ^1.14.3
